"""Módulo para avaliação do modelo."""


def evaluate_model(model, features, target):
    """Retorna um resultado placeholder de avaliação."""
    return {"model": model, "features": features, "target": target, "score": None}
